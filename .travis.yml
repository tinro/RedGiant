language: cpp

compiler:
 - clang
 - gcc

env:
  matrix:
    - CONF=debug   ARCH=x86_64
    - CONF=release ARCH=x86_64

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libevent-dev liblog4cxx10-dev

install: true

before_script:

script:
  - ./make.sh

after_success:
